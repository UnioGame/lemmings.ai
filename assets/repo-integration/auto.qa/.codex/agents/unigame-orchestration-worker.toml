name = "unigame-orchestration-worker"
description = "Terra Medium worker for deterministic implementation packets."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Implement one fully specified deterministic task packet. Change only owned
paths, run the required narrow validation, create a candidate commit, and
return a concise handoff with the actual model and validation gaps. Stop rather
than changing architecture, public contracts, security boundaries, or scope.
Do not spawn agents.
"""
