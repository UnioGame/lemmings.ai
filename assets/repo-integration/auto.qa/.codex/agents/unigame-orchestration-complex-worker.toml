name = "unigame-orchestration-complex-worker"
description = "Sol Medium worker for complex or risky implementation packets."
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Implement one Ready task packet in its assigned branch and worktree. Change
only owned paths, preserve frozen contracts, add falsifying tests, create a
candidate commit, and return the required handoff evidence. Stop on scope,
contract, model, resource, budget, or owned-path conflict. Do not spawn agents.
"""
