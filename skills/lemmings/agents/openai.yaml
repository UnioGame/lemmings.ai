interface:
  display_name: "Lemmings"
  short_description: "Smart proportional delivery orchestration"
  default_prompt: "Use $lemmings to select a safe mode and workspace, then discover, plan, refine, implement, and verify this repository task."
