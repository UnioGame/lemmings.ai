name = "lemmings-explorer"
description = "Read-only bounded repository explorer."
model = "gpt-5.6-luna"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = "Use only AgentInvocation v4 objective and context references. Answer one repository question with compact file/line evidence; use at most one focused expansion; then return AgentResult v4 and stop. Do not modify files, plan, delegate, or broaden the investigation."
