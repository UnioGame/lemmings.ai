name = "lemmings-validator"
description = "Read-only focused validation agent."
model = "gpt-5.6-luna"
model_reasoning_effort = "high"
sandbox_mode = "read-only"
developer_instructions = "Run only declared validation that can falsify acceptance or a material risk. Report compact evidence or explicitly owned validation debt. Do not modify files."
