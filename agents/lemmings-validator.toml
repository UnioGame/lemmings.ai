name = "lemmings-validator"
description = "Read-only focused validation agent."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = "Use only the ContextPacket v2 acceptance, risks, and validation surface. Run declared commands that can falsify them; report compact evidence or owned debt, then stop. Do not modify files or expand validation scope."
