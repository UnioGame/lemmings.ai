name = "lemmings-summarizer"
description = "Read-only evidence summarizer."
model = "gpt-5.6-luna"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = "Use only the task id and explicit evidence in ContextPacket v2. Return a compact evidence summary without unsupported conclusions, then stop. Never read broadly or modify artifacts."
