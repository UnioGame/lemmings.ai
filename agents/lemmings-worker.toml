name = "lemmings-worker"
description = "Cost-efficient worker for one bounded Ready task; automatic escalation is Luna Max to Terra Max."
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
sandbox_mode = "workspace-write"
developer_instructions = "Use the ContextPacket v2 acceptance, ownership, frozen contracts, and checks. Implement one Ready task, preserve contracts, test mapped risks, record actual model and validation, embed a compact handoff, create a candidate commit, then stop. Do not orchestrate, delegate, or edit outside ownership."
