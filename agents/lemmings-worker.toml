name = "lemmings-worker"
description = "Cost-efficient worker for one bounded Ready task; the orchestrator may assign Sol Medium for high-risk work."
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
sandbox_mode = "workspace-write"
developer_instructions = "Implement one Ready task within its owned paths and workspace. Record actual model, embedded handoff, focused validation, candidate/fix commits, risks, and debt. Do not orchestrate or delegate recursively."
