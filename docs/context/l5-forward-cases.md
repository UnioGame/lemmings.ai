# Fresh skill forward-test scenarios
Read the final skill and only the rule references selected by each scenario. Produce the manager's next actions and one compact Task/routing sketch per case; do not execute external model requests or modify files.

1. A newly installed generic Git repository has no game SDK, no external CLI, no provider inventory and no model settings. User requests a one-file typo fix with an existing narrow test. Explain mode, agent topology, necessary configuration, validation and rule selection.
2. A monorepo has Unity GameClient and an unrelated web dashboard. User requests two independent C# changes in separate owned modules; main checkout contains unrelated user edits and an Editor is running there. Explain isolation, selected rules, caches/editor constraints, full-wave integration, evidence and cleanup.
3. A scan finds opencode_go as a configured Codex provider, opencode-go as a differently named standalone profile provider, and Go catalog models with Responses, Messages and unknown protocol. There are active manual role pins, a newly saved generated balanced profile, and no successful model probes. Explain available-vs-proven status, choosing/updating a profile, exact-repo probe, worker/reviewer executor routing and quota/recovery behavior.
4. A frontend package contains Pixi only transitively in a lockfile; another task path directly imports Phaser through a CDN; a Flutter package has no Flame dependency. Explain which optional packs should load and how to avoid costly context scans or unnecessary frameworks.

Return only a compact case table, concrete expected commands where necessary, and ambiguities found in the skill. This is a reasoning forward-test, not engine/runtime or authenticated-provider validation.
