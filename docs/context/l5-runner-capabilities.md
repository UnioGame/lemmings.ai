# Runner capability evidence

Read-only local CLI help inspected 2026-09-10; no inference issued. Codex exec supports --profile (standalone CODEX_HOME/name.config.toml), --sandbox read-only|workspace-write, --ephemeral, --ignore-user-config (auth still CODEX_HOME), --ignore-rules, --output-schema FILE, --output-last-message FILE, --json, --cd and stdin prompt. Never use resume/fork/dangerously-bypass flags. Existing profile may depend on base provider config; preserve the selected provider connection while preventing unrelated plugins/MCP/nested delegation from granting effects. Unsupported enforcement must fail explicitly.

OpenCode run supports --pure (no external plugins), --agent, --format json, --model provider/model, --variant, --dir, --file and message/standard input. No --continue/--session/--attach/--share/--auto. Permissions/config reference: https://opencode.ai/docs/permissions/ and https://opencode.ai/docs/config/ . Environment OPENCODE_CONFIG_CONTENT can provide invocation-local role configuration. Explicit deny unsupported tools including delegation and mutations for readers; do not trust prompt-only read-only roles. Web/custom MCP calls are denied unless a scoped contract authorizes them. Do not install or reconfigure global providers from runner.

Only selected route fields are execution inputs. No generation calls during offline tests. Fake-process tests exercise lifecycle locks, timeout/cancel termination, JSON result identity, bounded diagnostics and exact role restrictions. Parent CLI verifies saved invocation/revision/assigned route and accepts result separately.

Registry seam for actual process safety: workspace._registry_lock(repo), load_registry(repo), and _save_registry(repo,registry,expected_revision) keep their existing signatures. For a managed entry matching canonical repo path, require entry.taskId == invocation.taskId and no foreign activeInvocationId/processes, then reserve activeInvocationId before process creation. Publish {pid,invocationId,status,startToken} in processes. Clear only this invocation's ownership after confirmed process/tree termination; preserve blocked or ambiguous ownership. No registry entry means ordinary current/native scope; still use per-workspace run lock. This connects external runs to workspace claim/release/remove and prevents the two modules from having independent notions of live writers. Regression fixture must prove a live registered invocation blocks removal, and failed termination leaves evidence.

Source bundle helper is lemmings.bundle.skill_root(repo); it resolves source layout or installed repo skill. Use source relative context paths for saved invocations, never full configs or credential-containing launch environments. Parent run CLI verifies Task lifecycle/revision/plan/context/profile and assigned role route before invoking adapter; the adapter still validates its own role/protocol/capabilities.

Parent CLI additionally checks worker repo against recorded workspace.destination and HEAD against baseSha (or the latest recorded candidate/fix for Repair) before launch. Runner tests need not duplicate full Task reconstruction; focus process/permissions and result contracts.
