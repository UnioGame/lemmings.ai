# Final scenario verification

User authorized sequential execution and manager self-review after host reviewer-slot failure. These are manager forward checks plus executable fixtures, not fresh-agent or paid-provider claims.

| Scenario | Expected behavior | Evidence |
| --- | --- | --- |
| Generic one-file fix | Auto resolves Simple; current host, no scan/SDK/preset; one narrow check; no engine refs | zero-config and CLI workflow fixtures; final SKILL/defaults inspected |
| Two Unity modules, dirty main checkout | Strict; independent package/code worktrees; task-scoped Unity pack; separate writable caches and editor project paths; integrate complete wave; canonical proof before cleanup | workspace safety, nearest-project rule and exact integration fixtures |
| Go aliases and saved profile | Exact provider spelling; catalog/auth/compatibility are separate; manual role pins beat generated selection; save does not select; explicit probe only; Responses Codex/OpenCode, Messages OpenCode, unknown blocked | discovery/profiles/frozen invocation fixtures; no paid model call |
| Transitive Pixi, direct CDN Phaser, Flutter without Flame | No Pixi pack from lockfile; Phaser pack for direct CDN task scope; Flutter pack only for SDK dependency; no framework added | test_rules.py direct/transitive/CDN/SDK fixtures and pack content review |

CLI help verified for rules explain --path/--technology/--platform and profiles/model commands. Seven packs contain concrete role/lifecycle/performance/context/validation guidance. Core stays usable with no selected engine and no external CLI. Remaining limits: real engine/device behavior and authenticated subscription generation are outside this Python/documentation verification; CLI call-count limits are instructions where not exposed by the host, while deadlines/process ownership are enforced.
