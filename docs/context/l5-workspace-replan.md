# Workspace completion replan
L5-A exhausted one repair. Fresh L5-A2 starts at bbb2d65d26e61eab3eb4abed3ddc13999a2e08d5; same workspace.py/test_workspace_safety.py ownership and APIs. Terra Max chosen for remaining lifecycle invariants; one repair budget.

1. Every claim, including same-task active, binds actual Git, caller AND original registry branch/base. entry.branch==requested branch and entry.baseSha==requested base for same-task idempotent claim. For code/package worktrees entry.sourceRepoRoot and repoRoot resolve to actual managed repo root; commonDir must equal actual managed repo commonDir, not self-consistent foreign metadata. Standalone clone backend must retain its distinct expected identity and approved source-root semantics. Preserve valid idempotent claims. Tests mutate stored branch alone, base alone, and a coherent foreign source/path/commonDir tuple while repoRoot is original; reject each.
2. Pooled idle entry (everActive or lastTaskId present / released pool state) removal and eviction require ALL stored lastTaskId, lastTaskPath, lastTaskRevision and mapping releaseEvidence before invoking any cleanup. Compare to exact canonical Task/id/path/revision/merge/command evidence. Missing fields or mismatches retain/quarantine, never fill new authority at removal. Fresh unactivated idle entry cannot skip lifecycle even if caller supplies unrelated Integrated Task. Record evidence only as part of successful original active release; valid pool removal remains supported.
3. Preserve passing 14 tests and add narrow regressions for each altered/missing field in direct remove and pool eviction; positive canonical release->remove test. No CLI/docs changes, no force/reset/clean/prune. Run focused tests and exact diffcheck. Independent review of immutable candidate and original inherited range; no repeated broad suite.
